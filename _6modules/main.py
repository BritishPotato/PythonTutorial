# python has a way to put definitions (custom functions) in a file and use them,
# this is called a module
# modules can be imported into other modules or into the main module

# main module being the collection of variables you have access to in a script
# executed at the top level and in calculator mode

# module contains Python definitions and statements.
# moduleName.py
# module name availavle at global variable __name__

# let's fucking try it out!

# - created fibo.py

import fibo, sex

# fib(100) --> NOPE, REMEMBER TO CALL MODULE!
fibo.fib(100) # yea boi

sex.sex()
